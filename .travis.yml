language: java
install: true

jdk:
  - oraclejdk8

sudo: false

script:
  - java -version
  - gradle --version
  - gradle wrapper --gradle-version 4.9
  - gradle --version

notifications:
  email: false